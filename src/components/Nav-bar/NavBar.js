// home || login || signup page || dashboard || session view
const Navbar = () => {
  return (
    <div className="bg-gray-800 text-red-400 p-4 font-bold">
      <h1>Im the nav</h1>
    </div>
  );
};

export default Navbar;
